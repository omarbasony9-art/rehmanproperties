Update the existing **Rehman INC admin dashboard**.

Do NOT rebuild the website.
Do NOT replace the existing architecture.
Do NOT add expensive services.
Do NOT change the public website design unless required for functionality.

There are two priorities:

1. The admin dashboard is currently extremely slow.
2. Fix the configuration warnings currently showing in the admin.

---

# 1. MAKE THE ADMIN DASHBOARD MUCH FASTER

The admin should feel fast and responsive.

Right now navigating between Dashboard, Inquiries, Website Content, Images, Settings, etc. takes too long.

First profile the current admin and determine what is causing unnecessary requests, repeated queries, or blocking renders.

Then optimize it.

## ADMIN NAVIGATION

When I click between admin sections:

* Do not reload the entire application.
* Do not refetch every piece of admin data.
* Do not rerun expensive database queries that the current page does not need.
* Do not reload authentication/session state unnecessarily.
* Do not reload all inquiries every time I change pages.

Use client-side routing correctly.

The admin shell/sidebar/header should stay mounted while only the page content changes.

---

# LOAD DATA ONLY WHEN NEEDED

Each admin page should only fetch the data that it actually needs.

Examples:

Dashboard:

* Fetch dashboard statistics
* Fetch only the most recent few inquiries

Inquiries:

* Fetch paginated inquiry data

Properties:

* Fetch properties only

FAQ:

* Fetch FAQs only

Images:

* Fetch media information only

Website Content:

* Fetch website content only

Settings:

* Fetch settings only

Do NOT have every admin route call one giant API endpoint that retrieves everything.

---

# INQUIRIES PERFORMANCE

Do not load every inquiry in the database at once.

Implement pagination.

Default:

`25 inquiries per page`

Options:

* 25
* 50
* 100

Database should handle:

* searching
* filtering
* sorting
* pagination

Do not download thousands of rows to the browser just to filter them in JavaScript.

Return:

* results
* current page
* total results
* total pages

---

# DATABASE INDEXES

Review the existing D1 schema.

Add appropriate indexes where useful for admin queries, particularly:

* inquiries.created_at
* inquiries.status
* inquiries.email
* inquiries.phone
* inquiry_number

Only add indexes that actually improve common queries.

Do not create unnecessary indexes.

---

# DASHBOARD STATS

Do not load every inquiry and count them in JavaScript.

Use efficient database aggregate queries such as:

`COUNT(*)`

and grouped status counts.

Create one lightweight dashboard stats API request.

---

# IMAGE PERFORMANCE

Property photos and website images should NOT make the admin slow.

Use:

* thumbnail-size previews where possible
* lazy loading
* `loading="lazy"`
* proper image dimensions
* no full-resolution images until I click them

Do not download 5–15 full-resolution property photos for every inquiry displayed in the table.

Only load full images when an inquiry detail or image viewer is opened.

---

# CACHING

Cache data that changes infrequently during the current admin session, such as:

* website settings
* contact information
* navigation configuration

When I save a change, invalidate/update that specific cached data.

Do not refetch everything.

Do not introduce an unnecessarily large caching framework if the current project can accomplish this cleanly with existing utilities.

---

# REQUESTS

Inspect the browser/network behavior.

Remove:

* duplicate API calls
* accidental request loops
* repeated `/auth/me` calls
* repeated settings requests
* repeated content requests
* requests caused by incorrect React effect dependencies

Make sure React development behavior is not causing production logic to make duplicate network calls.

---

# LOADING STATES

Do not block the entire admin screen while one small section loads.

Use:

* localized loading states
* lightweight skeletons
* cached existing data during refresh

Navigation should feel immediate.

Do NOT cover the whole admin with a spinner every time I click something.

---

# PERFORMANCE TARGET

Aim for:

* Admin route changes to feel nearly immediate
* Normal cached page switches under roughly 500ms where possible
* Database-backed pages generally usable within 1–2 seconds
* No obvious multi-second blank screens

Do not fake performance by hiding loading failures.

---

# 2. FIX THE CLOUDFLARE R2 WARNING

The Images admin currently displays:

`Cloudflare R2 Configuration Required`

and says these variables are missing:

* `R2_ACCOUNT_ID`
* `R2_ACCESS_KEY_ID`
* `R2_SECRET_ACCESS_KEY`
* `R2_BUCKET_NAME`
* `R2_PUBLIC_URL`

Handle this correctly.

## FIRST CHECK EXISTING CONFIGURATION

Inspect the current project/environment and determine whether an R2 bucket and credentials are already configured under different names.

If they already exist:

* Wire the existing environment variables into the image upload system.
* Do not ask me to create duplicate credentials.

If R2 is genuinely not configured:

DO NOT invent credentials.
DO NOT hardcode fake values.
DO NOT claim image uploads work.

Instead, clearly tell me at the end exactly which Cloudflare values I still need to provide and where to obtain them.

The required variables should be:

`R2_ACCOUNT_ID`

`R2_ACCESS_KEY_ID`

`R2_SECRET_ACCESS_KEY`

`R2_BUCKET_NAME`

`R2_PUBLIC_URL`

Keep:

`R2_SECRET_ACCESS_KEY`

as a SECRET.

Do not expose it to browser/frontend code.

---

# R2 IMAGE SYSTEM

Once R2 credentials exist, make the admin Images section actually functional.

Admin should be able to:

* Upload image
* Preview image
* Rename/display title if appropriate
* Delete image
* Select image for page hero
* Select image for website sections
* Use image for property listings

Property inquiry photos should also continue to use secure storage.

Validate:

* JPG
* JPEG
* PNG
* WEBP

Add reasonable upload size limits.

All credentials and signing should remain server-side.

---

# R2 PUBLIC URL

If the project needs public website images:

Use the configured public/custom R2 URL correctly.

Do not expose private R2 credentials.

If property inquiry photos are intended to remain private, do NOT automatically make those uploads publicly enumerable.

Separate public website media from private seller inquiry photos where appropriate.

---

# REMOVE THE WARNING AFTER IT IS ACTUALLY FIXED

The orange:

`Cloudflare R2 Configuration Required`

box should automatically disappear when the required configuration is present and validated.

Do NOT simply hide the warning while image uploads remain broken.

If configuration is still missing, keep a concise configuration message for administrators.

---

# 3. FIX THE INFRASTRUCTURE SECRETS WARNING/NOTICE

The admin currently displays an orange message saying:

`Infrastructure secrets (RESEND_API_KEY, SESSION_SECRET, database credentials) are managed in Replit environment variables and are not shown here.`

This is not useful as a large warning and makes the admin look unfinished.

Remove this large orange warning box from the normal Website Settings UI.

Infrastructure secrets should simply remain outside of the admin dashboard.

The admin does NOT need to display or expose:

* RESEND_API_KEY
* SESSION_SECRET
* R2_SECRET_ACCESS_KEY
* database credentials
* API credentials

Do not show their values.

Do not provide an admin interface for viewing them.

If necessary, replace the large orange warning with a very small neutral informational line such as:

`Secure infrastructure settings are managed separately.`

But preferably do not show anything unless there is an actual configuration problem.

---

# CONFIGURATION HEALTH

Create a small **System Status** section in Admin Settings.

Do NOT expose secret values.

It can safely display only:

Email Notifications: `Configured` / `Needs Setup`

Image Storage: `Configured` / `Needs Setup`

Database: `Connected` / `Error`

Admin Authentication: `Configured`

For example:

✓ Email notifications — Ready
✓ Database — Connected
✓ Admin authentication — Ready
⚠ Image storage — Needs configuration

This is much cleaner than showing giant warning boxes throughout unrelated admin pages.

Never reveal API keys or credentials.

---

# EMAIL SYSTEM

Do not break the existing Resend configuration.

Keep:

`RESEND_API_KEY`

Keep:

`RESEND_FROM_EMAIL=Rehman INC <inquiries@therehmanproperties.com>`

Keep:

`INQUIRY_NOTIFICATION_EMAIL=Aliproperties91@gmail.com`

Every inquiry must still:

1. Save to the database.
2. Appear in Admin → Inquiries.
3. Send an email notification to `Aliproperties91@gmail.com`.

---

# DO NOT BREAK

Do not break:

* Admin login
* Inquiry storage
* Inquiry email notifications
* Admin notes
* Status changes
* Website content editing
* Properties management
* FAQ management
* Contact information
* Ali Rehman contact information
* Instagram link
* Existing public website
* Mobile layouts

---

# KEEP IT CHEAP

Continue using the current architecture.

Do NOT add:

* Firebase
* Supabase
* Clerk
* Auth0
* PostgreSQL
* OpenAI
* paid CMS
* paid image service
* unnecessary third-party APIs

Cloudflare R2/D1 and the existing services should remain the architecture.

---

# TEST EVERYTHING

Before saying this is complete:

1. Run TypeScript typecheck.
2. Run production build.
3. Measure/check admin network requests.
4. Remove duplicate requests.
5. Test Dashboard speed.
6. Test Inquiries speed.
7. Test Website Content speed.
8. Test Properties speed.
9. Test FAQ speed.
10. Test Images speed.
11. Test Settings speed.
12. Verify pagination.
13. Verify database indexes/migrations.
14. Verify images lazy-load.
15. Verify admin navigation does not reload the entire app.
16. Verify email system still works.
17. Verify inquiries are still saved.
18. Verify no secrets are exposed to the frontend.
19. Confirm whether R2 is fully configured.
20. If R2 still needs credentials from me, give me the exact missing variables and instructions instead of pretending it is fixed.

Make these optimizations now.

Do not just hide errors or warnings. Fix the underlying causes when possible.
